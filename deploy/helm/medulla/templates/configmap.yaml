apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "medulla.fullname" . }}-config
  labels: {{- include "medulla.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
